version: "3"
services:
  web:
    build: .
    ports:
      - "8050:8050"
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
      DATABASE_PARAMS: Driver={ODBC Driver 17 for SQL Server};Server=tcp:db,1433;Database=sqldb-web-prod-001;Uid=sa;Pwd=Cl1mat3m1nd!;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;
  db:
    image: "mcr.microsoft.com/mssql/server"
    environment:
      SA_PASSWORD: "Cl1mat3m1nd!"
      ACCEPT_EULA: "Y"
