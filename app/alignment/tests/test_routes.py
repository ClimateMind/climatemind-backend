# TODO:
def test_post_shared_solution_selection(auth, db_session):
    pass
    # invalid_uuid = "invalid_uuid"
    # response = client.post(f"/alignment/{invalid_uuid}/shared-solutions")
    # assert response.status_code == 400
    # assert "error" in response.get_json()
    # assert "ALIGNMENT_SCORES_UUID" in response.get_json()["error"]
